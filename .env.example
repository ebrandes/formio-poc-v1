# Formio API Configuration
VITE_FORMIO_API_URL=http://localhost:3001
VITE_FORMIO_PROJECT_URL=http://localhost:3001

# Formio Admin Credentials (for Docker setup)
# IMPORTANT: Use simple password for development, change for production
FORMIO_ADMIN_EMAIL=admin@example.com
FORMIO_ADMIN_PASSWORD=CHANGEME

# MongoDB Configuration (for Docker)
MONGO_USERNAME=admin
MONGO_PASSWORD=password
MONGO_DATABASE=formio

# JWT Secret (generate a random string for production)
JWT_SECRET=CHANGEME_RANDOM_SECRET_KEY_123456789

# MongoDB Root Credentials (optional)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password